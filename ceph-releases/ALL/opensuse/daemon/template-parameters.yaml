template:
  repos:
    build-deps:
      http://download.opensuse.org/repositories/filesystems:/ceph/{{ template["os-repo"] }}/
    cloud-tools:
      http://download.opensuse.org/repositories/Cloud:/Tools/{{ template["os-repo"] }}/
